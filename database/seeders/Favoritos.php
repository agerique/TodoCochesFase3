<?php

namespace Database\Seeders;

use App\Models\Coche;
use App\Models\Usuario;
use Illuminate\Database\Seeder;

class Favoritos extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        // $favoritos = new Favoritos;
        // $favoritos->id_usuario = Usuario::find(1)->id_usuario;
        // $favoritos->id_coche = Coche::find(2)->id_coche;
        // $favoritos->save();

        // $favoritos2 = new Favoritos;
        // $favoritos2->id_usuario = Usuario::find(2)->id_usuario;
        // $favoritos2->id_coche = Coche::find(1)->id_coche;
        // $favoritos2->save();
    }
}
